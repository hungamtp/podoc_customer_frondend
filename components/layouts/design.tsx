import { LayoutProps } from "@/models/index";
import Link from "next/link";
import * as React from "react";

export interface IDesignLayouyProps {}

export function DesignLayouy({ children }: LayoutProps) {
  return;
  <div>{children}</div>;
}
